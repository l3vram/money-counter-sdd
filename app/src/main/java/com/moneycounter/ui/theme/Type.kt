package com.moneycounter.ui.theme

import androidx.compose.material3.Typography

// El Luiso typography — Material 3 defaults (approximate Inter / Google Sans).
// Usage hierarchy:
//   displayLarge  → branding, empty states
//   headlineSmall, titleLarge → screen titles, section headers
//   bodyLarge, bodyMedium → information text, descriptions
//   labelMedium, labelSmall → buttons, labels, captions
val LuisoTypography = Typography()
